# app launcher and app launcher
bind=SUPER,t,spawn,kitty    
bind=SUPER,space,spawn,fuzzel

# apps
bind=SUPER,w,spawn,zen-beta
bind=SUPER,e,spawn,Thunar
#bind=SUPER,e,spawn,~/.config/mango/scripts/thunar.sh
bind=ALT,n,spawn,flatpak run md.obsidian.Obsidian


#########################
### WINDOW MANAGEMENT ###
#########################

bind=SUPER,q,killclient
bind=ALT,Tab,toggleoverview


bind=SUPER,g,toggleglobal
bind=ALT,v,togglefloating
bind=ALT,a,togglemaximizescreen

bind=SUPER,f,togglefullscreen
bind=ALT,f,togglefakefullscreen

bind=SUPER,i,minimized
bind=SUPER+SHIFT,I,restore_minimized


### Mouse Bindingsdata
# btn_left and btn_right can't bind none mod key
mousebind=SUPER,btn_left,moveresize,curmove
#mousebind=NONE,btn_middle,togglemaximizescreen,0
mousebind=SUPER,btn_right,moveresize,curresize

### Floating Window

# movewin
bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0

# resizewin
bind=CTRL+ALT,Up,resizewin,+0,-50
bind=CTRL+ALT,Down,resizewin,+0,+50
bind=CTRL+ALT,Left,resizewin,-50,+0
bind=CTRL+ALT,Right,resizewin,+50,+0


########################
### Focus & Movement ###
########################

bind=SUPER,Tab,focusstack,next

bind=ALT,Left,focusdir,left
bind=ALT,Right,focusdir,right
bind=ALT,Up,focusdir,up
bind=ALT,Down,focusdir,down

# Swap window
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right


#######################
### Tags & Monitors ###
#######################

# tag switch
bind=SUPER,Left,viewtoleft,0
bind=SUPER,Right,viewtoright,0
#bind=CTRL,Left,viewtoleft_have_client,0
#bind=CTRL,Right,viewtoright_have_client,0
bind=SUPER+CTRL,Left,tagtoleft,0
bind=SUPER+CTRL,Right,tagtoright,0

bind=Alt,1,view,1,0
bind=Alt,2,view,2,0
bind=Alt,3,view,3,0
bind=Alt,4,view,4,0
bind=Alt,5,view,5,0
bind=Alt,6,view,6,0
bind=Alt,7,view,7,0
bind=Alt,8,view,8,0
bind=Alt,9,view,9,0

# tag: move client to the tag and focus it
bind=SUPER,1,tag,1,0
bind=SUPER,2,tag,2,0
bind=SUPER,3,tag,3,0
bind=SUPER,4,tag,4,0
bind=SUPER,5,tag,5,0
bind=SUPER,6,tag,6,0
bind=SUPER,7,tag,7,0
bind=SUPER,8,tag,8,0
bind=SUPER,9,tag,9,0

# tagsilent: move client to the tag and not focus it
bind=SUPER+SHIFT,1,tagsilent,1,0
bind=SUPER+SHIFT,2,tagsilent,2,0
bind=SUPER+SHIFT,3,tagsilent,3,0
bind=SUPER+SHIFT,4,tagsilent,4,0
bind=SUPER+SHIFT,5,tagsilent,5,0
bind=SUPER+SHIFT,6,tagsilent,6,0
bind=SUPER+SHIFT,7,tagsilent,7,0
bind=SUPER+SHIFT,8,tagsilent,8,0
bind=SUPER+SHIFT,9,tagsilent,9,0

# monitor switch
bind=SUPER+Alt,Left,tagmon,left
bind=SUPER+Alt,Right,tagmon,right

#didnt work somewhat
#bind=SUPER+Alt,Left,tagmon,left
#bind=SUPER+Alt,Right,tagmon,right


###############
### Layouts ###
###############

# switch and set layouts
bind=SUPER+SHIFT,s,switch_layout

### scroller layout
bind=SUPER+SHIFT,f,setlayout,scroller

bind=ALT,e,set_proportion,1.0
bind=ALT,x,switch_proportion_preset,

bind=alt+super+ctrl,Left,scroller_stack,left
bind=alt+super+ctrl,Right,scroller_stack,right
bind=alt+super+ctrl,Up,scroller_stack,up
bind=alt+super+ctrl,Down,scroller_stack,dow

### dwindle layout

#dwindle layout(manual split mode)
bind=alt+shift,Return,dwindle_toggle_split_direction


######################
### Noctalia Shell ###
######################

bind=ALT,space,spawn,qs -c noctalia-shell ipc call launcher toggle
bind=SUPER,s,spawn,qs -c noctalia-shell ipc call controlCenter togglee
bind=SUPER,comma,spawn,noctalia-shell ipc call settings toggle

# bind=NONE,XF86AudioRaiseVolume,spawn,qs -c noctalia-shell ipc call volume increase
# bind=NONE,XF86AudioLowerVolume,spawn,qs -c noctalia-shell ipc call volume decrease
# bind=NONE,XF86AudioMute,spawn,qs -c noctalia-shell ipc call volume muteOutput
# bind=NONE,XF86MonBrightnessUp,spawn,qs -c noctalia-shell ipc call brightness increase
# bind=NONE,XF86MonBrightnessDown,spawn,qs -c noctalia-shell ipc call brightness decrease


###################
### Screenshots ###
###################

bind=NONE,Print,spawn_shell,grim $HOME/Pictures/Screenshots/$(date +%Y%m%d%H%M%S).png

bind=SHIFT,Print,spawn_shell,g=$(slurp -d) && [ -n "$g" ] && grim -g "$g" $HOME/Pictures/Screenshots/$(date +%Y%m%d%H%M%S).png

bind=ALT,Print,spawn_shell,grim -c $HOME/Pictures/Screenshots/$(date +%Y%m%d%H%M%S).png

bind=CTRL,Print,spawn_shell,f=$(mktemp -t screenshot-XXXXXX.png) && grim "$f" && wl-clipboard < "$f" && rm -f "$f"

bind=SUPER,Print,spawn_shell,f=$HOME/Pictures/Screenshots/$(date +%Y%m%d%H%M%S).png && grim "$f" && satty --filename "$f" --output-filename "$f" --actions-on-enter save-to-file --early-exit




##############
### System ###
##############

# reload config
bind=SUPER,r,reload_config  

# kill
bind=SUPER,q,killclient
# exit
bind=SUPER,m,quit
